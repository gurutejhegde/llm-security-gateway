[project]
name = "llm-security-gateway"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "gurutejhegde", email = "gurutej.dev@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
    "uvicorn[standard]>=0.52.1",
]

[project.scripts]
llm-security-gateway = "llm_security_gateway:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
